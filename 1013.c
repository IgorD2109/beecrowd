#include <stdio.h>
#include <stdlib.h>
int main() {
    int a, b, c, MAIORAB;

    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);

    MAIORAB = (a + b + abs * (a - b)) / 2;

    printf("%d eh o maior", MAIORAB);

    return 0;
}
